.. ==================================================
.. FOR YOUR INFORMATION
.. --------------------------------------------------
.. -*- coding: utf-8 -*- with BOM.

.. include:: ../Includes.txt


.. _admin-manual:

Administrator Manual
====================

Target group: **Administrators**

Actually in the TYPO3 side there should not be any more to do than to install the extension and maybe configure
the memcached server-settings.

.. _admin-configuration:

Configuration
-------------

Actually this extension only extends the existing memcached-cache-backend. The configuration is exactly the same.
:ref:`Memcached Backend Configuration <t3api:caching-backend-memcached>`